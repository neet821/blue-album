# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=blue_local_db

# JWT Configuration
SECRET_KEY=change-this-to-a-secure-random-string-at-least-32-characters-long
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Configuration (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:8080,http://127.0.0.1:5173

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Environment Type (development, production, docker)
# Leave empty for auto-detection
ENVIRONMENT=

# Docker-specific (only for Docker deployment)
DOCKER_ENV=false

# Logging
LOG_LEVEL=INFO
